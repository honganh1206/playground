---
id: The net package
aliases:
  - The net package
tags: []
---

# The net package

Provide components for building networked client and server programs that communicate over TCP, UDP or UNIX domain sockets

`Listen()` function listens for incoming connections on a specified network port.

`Accept()` blocks until an incoming connection request is made
