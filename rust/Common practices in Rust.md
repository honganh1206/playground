Use `assert!` macro to say something is true or `assert_eq!` for equivalence

```rust
assert_eq!(text.join(" "), "Hello world");
```

To implicitly return a result, we *omit the `;` from the last expression*.